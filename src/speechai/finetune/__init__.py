"""Fine-tuning package: LoRA training of Whisper on bank-domain audio.

Entry point: ``speechai-finetune`` or ``python -m speechai.finetune.train``.
Requires the ``finetune`` extra: ``pip install -e '.[finetune]'``
(see ``docs/finetuning.md`` for the full recipe).
"""
