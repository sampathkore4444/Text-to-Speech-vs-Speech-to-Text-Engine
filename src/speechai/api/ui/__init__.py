"""Browser demo console assets.

``index.html`` in this directory is the single-file demo UI served at ``/``.
It is declared as package data in ``pyproject.toml`` so it ships with the
installed package (e.g. in the Docker image), not just in editable installs.
"""
